apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - gitea-provider.secret.yaml
# Uncomment to apply value overrides generated from Flux substitutions
# patchesStrategicMerge:
#   - patches/authentik-values-patch.yaml
